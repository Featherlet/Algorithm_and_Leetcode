#include<string>
#include<vector>
using namespace std;

class Solution {
public:
	bool isMatch(string s, string p) {
		
		/*vector<vector<int>> opt(p.size, vector<int>(s.size()));
		if(s[0] != p[0])
			if(s[0] != '*')*/
		return true;
	}
};